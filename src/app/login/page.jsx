import Input from "@/components/Input";
import Login from "@/components/Login";

const LoginPage = () => {
  return (
    <section className="login-page min-h-screen flex justify-center items-center">
      <Login />
    </section>
  );
};

export default LoginPage;
